

export const WeatherMaker = () => {

    return(
        <div style={{ display: 'flex', justifyContent: 'center' }}>
            {/* Weather Maker */}

            <button style={{

            }}
                onClick={(event) => makeItRain}
            >
                Make it rain
            </button>

        </div>
    )

}